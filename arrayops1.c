#include<stdio.h>
#include ".\lib\arrayOP.h"
#define MAX 10
 int main(){
    int a[MAX] = {0};
    int i;
    for(i=0; i< MAX; i++){
        a[i]= i*i;
    }
    printIntArray(a, sizeof(a)/sizeof (a[i]) );

 }
